<h1>Roles list</h1>
<style type="text/css">
	ul {
		padding: 0;
		list-style: none;
	}

	li {
		padding: 10px;
		border: solid 1px #be0034;
		width: 250px;
		text-align: center;
		margin: 10px;
		color: black;
		background-color: #be0034;
		transition: all .3s ease;
	}

	li:hover {
		background-color: white;
	}

	a {
		cursor: pointer;
		text-decoration: none;
		color: white;
		transition: all .3s ease;
	}

	a:hover {
		color: #be0034;
	}
</style>
<ul>	
	<% @users.each do |u| %>
		<li><%= link_to "#{u.name} #{u.user_role.name} #{(seg = u.business_segment).nil? ? '' : "— " + seg.name + " segment"}", :controller => "client_ui", :action => "view", :user_id => u.id %></li>
	<% end %>
</ul>